#Create a function called greet that takes a name as parameter and prints "Hello, " followed by the name. 
# #Make sure to use a sample value and output the result.
name = input(f"Enter a name: ")#string variable
def greet(name):
    return (f"Hello, {name}")


print(greet(name))
