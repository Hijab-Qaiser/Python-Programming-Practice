#Ask the user to enter the length and width of a rectangle. Calculate and print the area.
length = int(input("Enter length of rectangle ")) #integer variable
width = int(input("Enter width of rectangle ")) #integer variable
print(f"Area of rectangle = {length * width}")

